VIDEO DEMO SCRIPT (UNDER 8 MINUTES)
Multimodal User Authentication and Product Recommendation System

Target length: 7:00 to 7:30 minutes
Goal: Demonstrate full authorized transaction and unauthorized denial smoothly in CLI.

=================================================================
A. BEFORE RECORDING (DO THIS FIRST)
=================================================================
1) Open terminal in project root.
2) Run exactly:

cd "/home/nanen-miracle-mbanaade/ML Code/ML_Multimodal-User-Auth-System"
source .venv/bin/activate

3) Confirm environment is active:

echo $VIRTUAL_ENV

You should see a path ending in .venv.

4) Optional quick check that key files exist:

ls data/processed
ls artifacts
ls reports

=================================================================
B. VIDEO SCRIPT WITH TIMESTAMPS
=================================================================

0:00 - 0:20  Opening
What to show:
- Terminal at project root
- Quick folder listing

Commands:

pwd
ls

What to say:
"This is our Multimodal User Authentication and Product Recommendation System. The system only shows product predictions after face and voice verification."


0:20 - 0:50  Show Project Components
What to show:
- Source scripts and output folders

Commands:

ls src
ls data
ls reports

What to say:
"The implementation includes data merge, image and audio feature extraction, three trained models, and a CLI simulation for authorized and unauthorized flows."


0:50 - 1:30  Show Metrics Evidence
What to show:
- Metrics file content

Command:

cat reports/metrics.json

What to say:
"These are the evaluation metrics for the three required models: facial recognition, voice verification, and product recommendation, reported using Accuracy, F1-score, and Loss."


1:30 - 3:40  Authorized Full Transaction Demo
What to show:
- Run authorized command
- Pause on identity, verification, and final product lines

Command:

python -m src.auth_system_cli --face-image "data/images/member_1/neutral.jpg" --voice-audio "data/audio/member_1/yes_approve.wav"

What to say while output appears:
"Step one: the system predicts face identity."
"Step two: product prediction is prepared only after face validation."
"Step three: voice identity is verified as second-factor authentication."
"Since both checks pass, the system approves access and displays the predicted product."

Important output lines to pause on:
- Face identity: member_1
- Voice identity: member_1
- VOICE VERIFIED
- DISPLAY PREDICTED PRODUCT


3:40 - 5:00  Unauthorized Demo
What to show:
- Run unauthorized command
- Pause on denial output

Command:

python -m src.auth_system_cli --face-image "data/images/unauthorized/unauthorized.jpg" --voice-audio "data/audio/unauthorized/unauthorized voice.wav"

What to say:
"Now we test the unauthorized scenario."
"The system should deny access when biometric validation fails."
"This confirms the denial branch in the multimodal logic."

Important output line to pause on:
- ACCESS DENIED


5:00 - 6:00  Show Generated Artifacts
What to show:
- Processed features, model artifacts, and report visuals

Commands:

ls data/processed
ls artifacts
ls reports

What to say:
"These are the generated deliverables: merged and engineered datasets, extracted image and audio features, trained model files, and report assets including metrics and visual outputs."


6:00 - 7:00  Rubric Alignment Closing
What to say:
"This demo satisfies system simulation requirements by showing both a full authorized transaction and an unauthorized attempt."
"All three models were implemented and evaluated with Accuracy, F1-score, and Loss."
"The interaction is smooth, reproducible, and aligned with the assignment workflow."


7:00 - 7:30  Optional Final Frame
What to show:
- Keep terminal visible with final successful outputs
- End recording cleanly

=================================================================
C. COMMANDS BLOCK (COPY-PASTE QUICK RUN)
=================================================================

cd "/home/nanen-miracle-mbanaade/ML Code/ML_Multimodal-User-Auth-System"
source .venv/bin/activate
cat reports/metrics.json
python -m src.auth_system_cli --face-image "data/images/member_1/neutral.jpg" --voice-audio "data/audio/member_1/yes_approve.wav"
python -m src.auth_system_cli --face-image "data/images/unauthorized/unauthorized.jpg" --voice-audio "data/audio/unauthorized/unauthorized voice.wav"
ls data/processed
ls artifacts
ls reports

=================================================================
D. TROUBLESHOOTING DURING RECORDING
=================================================================
If command fails with module error:
- Ensure venv is active:
  source .venv/bin/activate

If file not found error appears:
- Verify exact filenames:
  ls data/images/member_1
  ls data/images/unauthorized
  ls data/audio/member_1
  ls data/audio/unauthorized

If output scrolls too fast:
- Re-run command and read only key lines.

If time is running out:
- Keep only: metrics + authorized demo + unauthorized demo + final artifacts listing.
